javascript:(function(){    const checkboxes = document.querySelectorAll('input[type="checkbox"].isMonitoring');    checkboxes.forEach(checkbox => checkbox.checked = true);    const textareas = document.querySelectorAll(        '.CourseLogMessageToLecturer, .CourseLogNotesToLecturer, .AttendanceMessageToLecturer, .AttendanceNotesToLecturer'    );    textareas.forEach(textarea => textarea.value = "ok");    const submitButton = document.querySelector('#btnSave');    if (submitButton) {        submitButton.click();    } else {        console.error("Submit button not found!");    }})();